if ( <%= @item.user.items.length %> == 1 ) { $(location).attr('href', '<%= root_path %>') }
else{
	$("#todo-list").append(
		'<%= j render @item %>'
	);
	$("#item_name").val('');
	$("#item-" + <%= @item.id %>).css("visibility", "visible").hide().fadeIn(1066);
}